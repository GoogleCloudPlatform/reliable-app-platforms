apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: nginx-route
  namespace: nginx
spec:
  parentRefs:
  - name: istio-gateway
    namespace: gateways
  rules:
  - backendRefs:
    - group: net.gke.io
      kind: ServiceImport
      name: nginx-service
      port: 80