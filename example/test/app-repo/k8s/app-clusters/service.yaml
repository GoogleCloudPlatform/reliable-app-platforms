apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
--- 
kind: ServiceExport
apiVersion: net.gke.io/v1
metadata:
  name: nginx-service
  namespace: nginx